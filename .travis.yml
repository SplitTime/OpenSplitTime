language: ruby
cache: bundler
rvm:
  - 2.6.1
services:
  - postgresql
install:
  - bundle install --jobs=3 --retry=3 --deployment
  - yarn install
  - bundle exec rake db:setup
addons:
  chrome: stable
