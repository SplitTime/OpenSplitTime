<%= turbo_stream.append "course_group_finishers",
                        partial: "course_group_finisher",
                        collection: @presenter.filtered_finishers,
                        locals: { organization: @presenter.organization, course_group: @presenter.course_group } %>

<%= turbo_stream.replace "pager",
                         partial: "shared/pager",
                         locals: { next_page_url: @presenter.next_page_url } %>
