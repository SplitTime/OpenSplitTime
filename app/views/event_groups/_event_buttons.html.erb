<div class="btn-group">
  <% events.each do |event| %>
    <%= link_to event.guaranteed_short_name,
                request.params.merge(id: event.to_param),
                class: 'btn btn-sm btn-primary',
                disabled: event == @presenter.event %>
  <% end %>
</div>
