<%= turbo_stream.append "roster_entrants",
                        partial: "efforts/entrant_for_roster",
                        collection: @presenter.filtered_roster_efforts,
                        as: :effort,
                        locals: { presenter: @presenter } %>

<%= turbo_stream.replace "pager",
                         partial: "shared/pager",
                         locals: { next_page_url: @presenter.next_page_url } %>
