<% content_for :title do %>
  <% "OpenSplitTime: Sync Efforts - #{@presenter.event_group.name}" %>
<% end %>

<%= render "shared/mode_widget", event_group: @presenter.event_group %>
<%= render "event_groups/setup_header", presenter: @presenter, breadcrumbs: ["Sync Entrants"] %>

<article class="ost-article container">
  <%= render partial: "events/sync_efforts_card",
             locals: { presenter: @presenter },
             collection: @presenter.event_group.events.order(:scheduled_start_time),
             as: :event %>

  <hr/>
  <div class="row mb-3">
    <div class="col">
      <%= link_to "Back to setup",
                  setup_event_group_path(@presenter.event_group, display_style: :entrants),
                  class: "btn btn-outline-secondary" %>
    </div>
  </div>
</article>
