<%= turbo_stream.remove @event %>
<%= turbo_stream.replace dom_id(@presenter.event_group, "setup_widget"),
                         partial: "event_groups/setup_widget",
                         locals: { presenter: @presenter } %>
