<%# locals: (event_group:, event:, external_id:, external_name:) -%>

<%= turbo_stream.replace "toggle_event_#{event.id}_external_id_#{external_id}",
                         partial: "event_groups/connect_service/syncable_switch",
                         locals: {
                           event_group: event_group,
                           event: event,
                           external_id: external_id,
                           external_name: external_name,
                         } %>

<%= turbo_stream.replace dom_id(event, :preview_sync),
                         partial: "event_groups/connect_service/preview_sync_button",
                         locals: { event: event } %>
