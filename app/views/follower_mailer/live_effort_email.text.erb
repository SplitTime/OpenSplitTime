<%= "Hello #{@follower.first_name}," %>

<%= "The following new times were reported for #{@effort_data[:full_name]} at #{@effort_data[:event_name]}:" %>

<% @effort_data[:split_times_data].each do |split_time_data| %>
    <%= "#{split_time_data[:split_name]} at #{split_time_data[:day_and_time]}" %><%= " with a pacer" if split_time_data[:pacer] %><%= " and dropped there" if @effort_data[:dropped_split_id] == split_time_data[:split_id] %>

<% end %>