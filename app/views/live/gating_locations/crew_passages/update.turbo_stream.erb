<%= turbo_stream.replace dom_id(@board.gating_location_event, :crew_access_rows),
                         partial: "live/crew_access_boards/event_rows",
                         locals: { board: @board } %>
