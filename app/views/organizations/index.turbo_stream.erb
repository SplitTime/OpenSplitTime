<%= turbo_stream.append "organizations",
                        partial: "organization",
                        collection: @presenter.records_from_context,
                        as: :record %>

<%= turbo_stream.replace "pager",
                         partial: "pager",
                         locals: { presenter: @presenter } %>
