<%= turbo_stream.replace "flash", partial: "layouts/flash" %>

<% if defined?(@audit_presenter) %>
  <%= turbo_stream.replace "effort_audit_table",
                           partial: "split_times/effort_audit_table",
                           locals: { presenter: @audit_presenter } %>
<% end %>
