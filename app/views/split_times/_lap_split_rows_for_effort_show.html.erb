<%# locals: (presenter:) -%>

<tbody id="lap_split_rows_for_effort_show">
<%= render partial: "split_times/lap_split_row_for_effort_show",
           collection: presenter.lap_split_rows,
           as: :row,
           locals: { presenter: presenter } %>
</tbody>
