<%# locals: (presenter:) -%>

<tbody id="lap_split_rows_for_live_entry">
<% if presenter.present? %>
  <%= render partial: "split_times/lap_split_row_for_live_entry",
             collection: presenter.lap_split_rows,
             as: :row,
             locals: { presenter: presenter } %>
<% end %>
</tbody>
