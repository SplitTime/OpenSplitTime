<%# locals: (subscription:) %>

<%= turbo_frame_tag "form_modal" do %>
  <div class="modal-header">
    <div class="modal-title h4 fw-bold">Add a Subscription</div>
  </div>
  <div class="modal-body">
    <%= render "form", subscription: subscription %>
  </div>
<% end %>
