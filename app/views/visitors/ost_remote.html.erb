<% content_for :title do %>OST Remote Docs<% end %>

<div class="d-flex" id="wrapper" data-controller="sidebar" data-target="sidebar.wrapper">
  <%= render 'ost_remote_sidebar' %>

  <div id="page-content-wrapper">
    <article class="ost-article container">
      <%= render 'ost_remote_header' %>
      <%= render "ost_remote_#{params[:topic] || 'setup'}_#{params[:page] || 1}" %>
    </article>
  </div>
</div>
