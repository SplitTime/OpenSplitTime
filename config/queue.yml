default: &default
  dispatchers:
    - polling_interval: 1
      batch_size: 500
  workers:
    # High priority: default queue for most jobs
    - queues:
        - default
        - mailers
      threads: 3
      processes: <%= ENV.fetch("JOB_CONCURRENCY", 1) %>
      polling_interval: 0.1
    # Lower priority: storage operations (analysis, purge)
    - queues:
        - storage
        - solid_queue_recurring
      threads: 1
      processes: 1
      polling_interval: 1

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
